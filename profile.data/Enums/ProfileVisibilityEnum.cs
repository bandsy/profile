namespace profile.data.Enums {
    public enum ProfileVisibilityEnum {
        //show to logged all inc. logged out users
        everyone,

        //show to only logged in users
        users,

        //show to no one 
        none
    }
}